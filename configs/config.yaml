dataset:
  gse: GSE249279
paths:
  raw_dir: data/raw
  interim_dir: data/interim
  processed_dir: data/processed
download:
  method: direct  # direct|geoparse (direct uses NCBI FTP HTTPS)
  files:
    - GSE249279_RAW.tar
    - GSE249279_scRNA-seq_merged_raw_counts.h5
